---
outline: [2, 3]
---

# Flex ordering

Based on the column scale in _config, also accessible with the `crayon.order(n)` mixin, which accepts arbitrary values for `n`

## Sass mixins

<!--@include: ../reference/mixins/flex-order.md-->

## Utility classes

<!--@include: ../reference/utility-classes/flex-order.md-->

## Example

```html
<div class="card">
  one
</div>
<div class="card order-first">
  two
</div>
```
